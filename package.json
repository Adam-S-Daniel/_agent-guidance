{
  "name": "agent-guidance",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only deps for scripts/check-cron-coverage.js, scripts/check-guidance-coverage.js and scripts/check-guidance-touch.js — see their headers.",
  "scripts": {
    "check-cron-coverage": "node scripts/check-cron-coverage.js",
    "check-guidance-coverage": "node scripts/check-guidance-coverage.js",
    "check-guidance-touch": "node scripts/check-guidance-touch.js"
  },
  "devDependencies": {
    "markdown-it": "15.0.1",
    "yaml": "2.9.0"
  }
}
